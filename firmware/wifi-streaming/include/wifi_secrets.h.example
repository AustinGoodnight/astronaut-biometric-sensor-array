// Copy this file to include/wifi_secrets.h (gitignored) and fill in your
// network's actual credentials. Kept out of version control since it's
// per-network, not per-machine like platformio.local.ini.

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
